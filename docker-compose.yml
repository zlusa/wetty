version: "3.8"
services:
  wetty:
    image: ghcr.io/butlerx/wetty:latest  # This will pull a pre-built image
    ports:
      - "3000:3000"
    environment:
      # Optional: Basic authentication
      # USERNAME: "your_username"
      # PASSWORD: "your_password"
    restart: unless-stopped
